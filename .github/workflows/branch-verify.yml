name: Verify PR
on:
  pull_request:
    types:
      - synchronize
      - opened

jobs:
  verify-pr:
    uses: entur/abt-gha-public/.github/workflows/gradle-open-source-verify.yml@v1.5
    # no secrets necessary
    permissions:
      contents: read