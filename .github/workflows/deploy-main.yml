name: Validate, build and deploy
on:
  push:
    branches:
      - main

jobs:
  gradle-build-and-release:
    uses: entur/abt-gha-public/.github/workflows/gradle-release-sona.yml@gradleReleaseBuild
    secrets: inherit
    with:
      channel-id: ${{ vars.CHANNEL_ID }}
      
